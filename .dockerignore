# Каталог модулей ставится внутри образа. Скопированный с хоста, он несёт чужую
# платформу и ссылки на несуществующий store, а pnpm на такой каталог просит
# подтверждения удаления и без TTY падает.
node_modules

.git
.github
log
tmp
storage
vendor/bundle
